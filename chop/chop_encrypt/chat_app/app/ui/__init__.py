# Import UI components to make them accessible from the 'ui' package.

from .main_window import MainWindow
from .dialogs import LoginDialog, ChatDialog

# You can add any UI-specific initialization code here if required.
